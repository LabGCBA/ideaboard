<% if persona_signed_in? %>
  <%= link_to('Salir', destroy_persona_session_path, :method => :delete, class: "btn-login") %>        
<% else %>
  <%= link_to('Iniciar Sesión', baid_login_path, class: "btn-login") %>  
<% end %>
